// Class is a template
class MyComplex
{
public:
    double real;
    double img;

    // Default constructor
    MyComplex();

    // Function to print complex number
    void print_add();
    void print_sub(); 

    void add(MyComplex, MyComplex);
    void sub(MyComplex, MyComplex);
};
